# Resolved Hydra Configuration (from smoke test run_20260322_201246)
# Source: verl trainer output at TaskRunner initialization

# ===== Key Prefix Optimization Config =====
# Extracted from log line 320-322
algorithm:
  optimize_prefix_tokens: True    # ← KEY: enabled prefix optimization
  prefix_loss_weight: 1.0        # ← KEY: prefix loss weight

# ===== Algorithm Config =====
# From log lines 308-330
algorithm:
  _target_: verl.trainer.config.AlgoConfig
  adv_estimator: grpo
  filter_groups:
    enable: false
    max_num_gen_batches: 0
  gamma: 1.0
  kl_ctrl:
    _target_: verl.trainer.config.KLControlConfig
    horizon: 10000
    kl_coef: 0.1
    target_kl: 0.1
    type: fixed
  kl_penalty: kl
  lam: 1.0
  norm_adv_by_std_in_grpo: true
  optimize_prefix_tokens: true    # KEY: prefix optimization enabled
  pf_ppo:
    reweight_method: pow
    weight_pow: 2.0
  prefix_loss_weight: 1.0         # KEY: prefix loss weight
  rollout_correction:
    bypass_mode: false
    rollout_is: null
    rollout_is_batch_normalize: false
    rollout_is_threshold: 2.0
    rollout_rs: null
    rollout_rs_threshold: null
    rollout_rs_threshold_lower: null
    rollout_token_veto_threshold: null
    use_policy_gradient: false
  use_kl_in_reward: false
  use_pf_ppo: false

# ===== Data Config =====
# From log lines 422-423, 479-481
custom_reward_function:
  name: compute_score
  path: null

data:
  filter_overlong_prompts: true
  max_prompt_length: 2048
  max_response_length: 128
  return_full_prompt: false
  return_multi_modal_inputs: true
  return_raw_chat: true
  return_raw_input_ids: false
  train_batch_size: 1
  train_files: /Data/wyh/datasets/Verl-Data/outputs/textcraft_old_logits/active/textcraft_validated_prefix_history_canonicalized_with_prefix_old_logprobs_step200_v2.parquet
  truncation: error
  val_files: /Data/wyh/datasets/Verl-Data/outputs/textcraft_old_logits/active/textcraft_validated_prefix_history_canonicalized_with_prefix_old_logprobs_step200_v2.parquet

# ===== Trainer Config =====
# From log lines 532-556
trainer:
  balance_batch: false
  default_local_dir: /Data/wyh/datasets/Verl-Data/outputs/textcraft_grpo_prefix_smoke_test_canonicalized
  device: cuda
  experiment_name: smoke_test
  logger:
    - console
  n_gpus_per_node: 1
  nnodes: 1
  project_name: textcraft_prefix
  save_freq: 999
  test_freq: 999
  total_epochs: 1
  val_before_train: false

# ===== Actor Config (Key Fields) =====
# From log lines 76-91, 100-104
actor_rollout_ref:
  actor:
    optim:
      lr: 1.0e-06
      weight_decay: 0.01
    ppo_epochs: 1
    ppo_micro_batch_size_per_gpu: 1
    ppo_mini_batch_size: 1
    fsdp_config:
      dtype: bfloat16
      optimizer_offload: true
      param_offload: true
      use_torch_compile: false

# ===== Rollout Config (Key Fields) =====
# From log lines
actor_rollout_ref:
  rollout:
    dtype: bfloat16
    gpu_memory_utilization: 0.3
    max_model_len: 4096
    max_num_batched_tokens: 256
    max_num_seqs: 4
    multi_turn:
      enable: true
      interaction_config_path: /Data/wyh/verl/examples/sglang_multiturn/config/interaction_config/textcraft_interaction.yaml
      max_assistant_turns: 25
      max_user_turns: 25
    n: 1
    name: vllm
    temperature: 0.7
    tensor_model_parallel_size: 1
    top_k: -1
    top_p: 0.9

# ===== Model Config =====
# From log lines 139-158
actor_rollout_ref:
  model:
    enable_gradient_checkpointing: true
    path: /Data/public/Qwen3-1.7B
    use_fused_kernels: true
    use_remove_padding: true

# ===== Validation Config Check =====
# From log lines 31-32
validate_config: All configuration checks passed successfully!
