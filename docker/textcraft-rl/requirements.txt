# ============================================================
# TextCraft RL 训练环境 - 额外 Python 依赖
# 注意：torch / vllm / flash-attn / flashinfer / ray 已在
#        vllm/vllm-openai:v0.11.0 基础镜像中预装，此处不再重复指定。
# ============================================================

# === Transformers 生态（verl 核心依赖）===
accelerate>=1.0.0
datasets>=2.20.0
peft>=0.12.0
trl>=0.8.0
transformers>=4.51.0    # 支持 Qwen3 需要 4.51+

# === 日志和监控 ===
wandb>=0.18.0

# === 配置管理 ===
hydra-core==1.3.2

# === 数据处理 ===
pandas>=2.0.0
numpy>=1.26.0,<2.0.0
scipy>=1.10.0
pyarrow>=19.0.0
matplotlib>=3.8.0

# === 工具库（verl 依赖）===
codetiming>=1.4.0
dill>=0.3.7
blobfile>=2.1.0
pybind11>=2.12.0

# === 加速下载 ===
hf-transfer>=0.1.5

# === 性能优化 ===
liger-kernel>=0.2.0
torch_memory_saver>=0.0.8
xgrammar>=0.1.0

# === 数学和文本处理（TextCraft / MATH 奖励函数）===
mathruler>=0.1.0
pylatexenc>=2.10

# === Qwen 多模态支持 ===
qwen_vl_utils>=0.0.8
